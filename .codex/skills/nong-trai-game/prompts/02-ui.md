# Milestone 02 — UI

Build the application UI around the farm.

Implement:

- top HUD: level, XP, coins
- farm page
- bottom/navigation area suitable for mobile
- inventory entry
- shop entry
- quests entry
- collection entry
- basic modal/toast primitives if needed.

Use the repo's existing UI conventions if present.

Do not implement real economy yet. Use typed mock state only where necessary.

Acceptance:

- responsive mobile/desktop
- clear hierarchy
- no duplicate state
- no console errors
- build passes.
