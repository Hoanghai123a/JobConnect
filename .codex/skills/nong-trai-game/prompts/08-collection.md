# Milestone 08 — Collection

Implement collection system.

Track:

- discovered
- harvestCount
- firstHarvestAt.

Build:

- collection list/grid
- locked/discovered state
- count
- first harvest metadata.

Collection must derive from actual player actions, not client-only display state.

Acceptance:

- first harvest discovers item
- repeated harvest increments count
- reload preserves collection.
