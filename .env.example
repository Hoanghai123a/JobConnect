# Server-side upstream for API proxy routes.
# Browser traffic should go through same-origin /api/public/pb.
PB_URL=https://your-pocketbase-domain.com

# Optional only if you need SSR/build-time direct access.
VITE_PB_URL=https://your-pocketbase-domain.com
